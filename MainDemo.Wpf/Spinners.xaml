<UserControl x:Class="MaterialDesignColors.WpfExample.Spinners"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaterialDesignColors.WpfExample"
             xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <StackPanel HorizontalAlignment="Stretch" Orientation="Vertical" VerticalAlignment="Stretch">
            <TextBlock Text="Click on the number to show the editing text box. Afterwards press Enter to apply the new value." />
            <WrapPanel HorizontalAlignment="Stretch" Margin="16" Orientation="Horizontal" VerticalAlignment="Stretch">
                <wpf:OversizedNumberSpinner />
                <wpf:OversizedNumberSpinner Margin="32,0,0,0" IsEnabled="False" Value="3" />
                <wpf:OversizedNumberSpinner Background="{DynamicResource SecondaryAccentBrush}" Foreground="{DynamicResource SecondaryAccentForegroundBrush}" Margin="32,0,0,0" Min="-3" Value="3" Max="15" />
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
